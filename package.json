{
  "name": "ts-boy",
  "version": "1.0.0",
  "description": "Gameboy emulator in TypeScript",
  "type": "module",
  "scripts": {
    "start": "http-server -c-1",
    "build": "mkdir -p dist && cp static/* dist && cp -r roms dist && tsc"
  },
  "author": "adrbin",
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "http-server": "^14.1.1",
    "typescript": "^5.7.2"
  }
}
